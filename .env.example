# Frontend (Vite) .env
# Copy to .env.local for local dev.

# OIDC (Authentik)
# Issuer should match the OIDC discovery issuer, typically:
# https://auth.example.com/application/o/<provider-slug>/
VITE_OIDC_ISSUER_URL=""
VITE_OIDC_CLIENT_ID=""
# Optional
VITE_OIDC_AUDIENCE=""
VITE_OIDC_SCOPE="openid profile email"
